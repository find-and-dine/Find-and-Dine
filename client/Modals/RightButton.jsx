import React from 'react';

const RightButton = ({ rightClick }) => (
  <button
    id="left_button"
    onClick={rightClick}
    type="button"
  >
    &#62;
  </button>
);

export default RightButton;
